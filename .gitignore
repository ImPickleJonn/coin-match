# Secrets — never commit
.env
.env.local
.env.*.local

# Runtime persistence (per-environment; Render mounts its own disk at /data)
data/*.json

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
